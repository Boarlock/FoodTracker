# Copilot Instructions

## Project Guidelines
- User clarified that the compilation error is specifically on Harmony's `codes[i - 1].IsLdloc(thingLocal)` call, not on `LocalVariableInfo`; preserve the .NET Standard 2.1 target and avoid suggesting a direct mscorlib reference.